# CLI File Organizer (Argparse)

This is a Python CLI tool that organizes files in a folder
using command-line arguments.

This version uses argparse instead of a menu.

## How to Run:

python file_organizer.py --path <folder_path> --mode move

Use --help to see all available options.


